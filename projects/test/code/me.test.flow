Flow me.test
     Basepath ""
// Show form to grab OP location
data = RRF "index.ftlh" 
// Populate external OP settings
obj = { host: data.host, dcr: { enabled: true, useCachedClient: true }, oauth: { scopes: ["openid" ] } }
// Launch the openid flow and grab its response
outcome = Trigger org.gluu.inbound.openid obj "goose"
// Authenticate the given user
it_cvsjn = {success:true, data: { userId: outcome.userId }}
Finish it_cvsjn